{
  "name": "@zonable/pathfinding",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cargo build --manifest-path Cargo.toml --target wasm32-unknown-unknown",
    "typecheck": "cargo check --manifest-path Cargo.toml"
  }
}
